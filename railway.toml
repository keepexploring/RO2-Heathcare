# Railway configuration for multi-service deployment
# Note: Deploy mosquitto separately or use external MQTT broker (HiveMQ Cloud)

[[services]]
name = "fastapi"
source = "fastapi"

[[services]]
name = "panel"
source = "panel"